<!doctype html>
<html lang="{{ page.seoLang }}">

	<head>
	
		{% include "head.twig" %}
		
		{% include basekit.headScript %}

	</head>

	<body id="image-fixed" class="{{ page.backgroundClass }} {{ site.colorSwatch }}">

		<div class="page-wrap">

			{% block content %}
			{% endblock %}

			{% include "footer.twig" %}

		</div>
		{% include basekit.bodyScript %}


		<script type="text/javascript">
			$(function() {
				$('a[href*=#]:not([href=#])').click(function() {
					if (location.pathname.replace(/^\//,'') == this.pathname.replace(/^\//,'') && location.hostname == this.hostname) {
						var target = $(this.hash);
						target = target.length ? target : $('[name=' + this.hash.slice(1) +']');
						if (target.length) {
							$('html,body').animate({
								scrollTop: target.offset().top
							}, 1000);
							return false;
						}
					}
				});
			});
		</script>
		<script type="text/javascript">
			!function() {
				function supportsSVG() {
					return !!document.createElementNS && !! document.createElementNS('http://www.w3.org/2000/svg', 'svg').createSVGRect
				}
				if (supportsSVG()) document.documentElement.className += ' svg'
			}();

			var headerHeight = document.getElementById("header-feature").offsetHeight,
				feature = $(".header-feature .widget.feature");
			window.onscroll = function emParallax() {
				console.log([feature, headerHeight]);
				if(typeof feature === 'object') {
					console.log([window.pageYOffset, headerHeight]);
					feature.height(((window.pageYOffset / 20) * 60) + headerHeight + "px");
				}
			}
		</script>

	</body>
</html>